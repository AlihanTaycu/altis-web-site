---
import Base from '../../layouts/Base.astro';
import HubCard from '../../components/sections/HubCard.astro';
import CTABanner from '../../components/sections/CTABanner.astro';
import {
  Camera, Lightbulb, QrCode, Store, Tag, CreditCard, ArrowRight,
} from '@lucide/astro';

const schema = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "name": "Altis Çözümler",
  "description": "Altis Teknoloji çözümleri: TrueScan, Pick-to-Light, Demirbaş Yönetimi, RFID Perakende ve Tag&Trace."
};

const metrics = [
  { value: '%99.5', label: 'barkod okuma doğruluğu', sub: 'TrueScan' },
  { value: '×2–3',  label: 'depo toplama hızı',      sub: 'Pick-to-Light' },
  { value: '%80',   label: 'demirbaş verimlilik',     sub: 'Demirbaş Yönetimi' },
];
---

<Base
  title="Çözümler | Altis Teknoloji"
  description="Depo, saha ve demirbaş operasyonları için Altis Teknoloji çözümleri: TrueScan, Pick-to-Light, Demirbaş Yönetimi ve daha fazlası."
  {schema}
  lang="tr"
  alternateUrl="https://www.altis.com.tr/en/solutions/"
>

<!-- ===== Hero ===== -->
<section class="sol-hero">
  <div class="sol-grid-bg" aria-hidden="true"></div>
  <div class="sol-glow-tl" aria-hidden="true"></div>
  <div class="sol-glow-br" aria-hidden="true"></div>

  <div class="container">
    <div class="sol-inner">
    <div class="eyebrow sol-eyebrow">Çözümler</div>
    <h1>Sahadaki işi hızlandıran, doğrulayan ve kayda alan uygulamalar.</h1>
    <p class="sol-lead">Altis çözümleri belirli operasyon problemlerine odaklanır — toplama hatasını azaltmak, varlıkları izlemek, okumayı güvenilir kılmak ve saha ekiplerine net iş akışı vermek.</p>

    <!-- Kategori pills -->
    <div class="sol-pills" data-reveal>
      <a href="/cozumler/goruntu-isleme/" class="sol-pill">
        <Camera size={14} stroke-width={2} />
        Görüntü İşleme
      </a>
      <a href="/cozumler/pick-to-light/" class="sol-pill">
        <Lightbulb size={14} stroke-width={2} />
        Depo Operasyonu
      </a>
      <a href="/cozumler/asset-yonetimi/" class="sol-pill">
        <QrCode size={14} stroke-width={2} />
        Demirbaş Takibi
      </a>
      <a href="/urunler/rfid-perakende/" class="sol-pill">
        <Store size={14} stroke-width={2} />
        Perakende
      </a>
    </div>

    <!-- Sonuç metrikleri -->
    <div class="sol-metrics" data-reveal data-reveal-delay="120">
      {metrics.map(m => (
        <div class="sol-metric">
          <span class="sol-metric-val" data-count={m.value}>{m.value}</span>
          <span class="sol-metric-label">{m.label}</span>
          <span class="sol-metric-sub">{m.sub}</span>
        </div>
      ))}
    </div>
    </div>
  </div>
</section>

<!-- ===== Çözüm kartları ===== -->
<section class="hub-list">
  <div class="container">
    <div class="solutions-grid" data-reveal>
      <div class="solution-tile solution-main">
        <HubCard
          variant="big"
          href="/cozumler/pick-to-light/"
          Icon={Lightbulb}
          tag="Depo operasyonu"
          title="Pick-to-Light"
          desc="Işıklı raf göstergeleriyle toplama, ayrıştırma ve hat besleme adımlarını operatöre tek tek gösterir — toplama hızını 2–3 katına çıkarır, hatayı en aza indirir."
        >
          <img
            slot="visual"
            class="p2l-card-visual"
            src="/images/pick-to-light/p2l-marketing.webp"
            alt=""
            width="1844"
            height="921"
            loading="lazy"
            decoding="async"
          />
        </HubCard>
      </div>

      <div class="solution-tile solution-side">
        <HubCard
          href="/cozumler/goruntu-isleme/"
          Icon={Camera}
          tag="Görüntü işleme"
          title="TrueScan"
          desc="Mevcut kameralara entegre AI okuma motoruyla kirli etiket ve düşük kontrastta %99.5 doğrulukla barkod ve 2D/QR okuma."
        >
          <video
            slot="visual"
            autoplay
            muted
            loop
            playsinline
            preload="metadata"
            poster="/images/truescan/truescan-marketing.webp"
            aria-label="TrueScan endüstriyel kamera ile kirli barkod ve QR kod okuma görseli"
            width="1844"
            height="921"
          >
            <source src="/videos/truescan-web-loop.mp4" type="video/mp4" />
          </video>
        </HubCard>
      </div>

      <div class="solution-tile solution-side">
        <HubCard
          href="/cozumler/asset-yonetimi/"
          Icon={QrCode}
          tag="Demirbaş takibi"
          title="Demirbaş Yönetimi"
          desc="NFC, QR ve mobil uygulama ile demirbaş, zimmet ve servis kayıtlarını sahadan yönet."
        >
          <div slot="visual" class="asset-mini-visual">
            <div class="asset-panel">
              <div class="asset-qr" aria-hidden="true">
                <span></span><span></span><span></span>
                <span></span><span></span><span></span>
                <span></span><span></span><span></span>
              </div>
              <div class="asset-list">
                <div class="asset-row active"><span>EL TERMINALI</span><strong>ZIMMETTE</strong></div>
                <div class="asset-row"><span>YAZICI</span><strong>BAKIM</strong></div>
                <div class="asset-row"><span>TABLET</span><strong>SAHADA</strong></div>
              </div>
            </div>
          </div>
        </HubCard>
      </div>

      <div class="solution-tile solution-banner">
        <HubCard
          variant="dark"
          href="https://roxcard.com.tr"
          external
          Icon={CreditCard}
          tag="Dijital Kimlik"
          title="Rox Card"
          desc="NFC ve QR teknolojisiyle çalışan yeni nesil dijital kartvizit platformu. Tek dokunuşla paylaş, panelden yönet."
          cta="roxcard.com.tr"
        />
      </div>
    </div>
  </div>
</section>
</Base>

<style>
/* ===== Hero ===== */
.sol-hero {
  position: relative;
  padding: 96px 0 72px;
  background:
    radial-gradient(900px 500px at 80% -80px, rgba(24, 183, 214, .10), transparent 60%),
    linear-gradient(180deg, #fff 0%, var(--bg-soft) 100%);
  overflow: hidden;
}
.sol-grid-bg {
  position: absolute;
  inset: 0;
  pointer-events: none;
  opacity: .35;
  background-image:
    linear-gradient(rgba(0, 18, 92, .05) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0, 18, 92, .05) 1px, transparent 1px);
  background-size: 56px 56px;
  -webkit-mask-image: radial-gradient(ellipse 80% 80% at 50% 0%, #000 40%, transparent 100%);
          mask-image: radial-gradient(ellipse 80% 80% at 50% 0%, #000 40%, transparent 100%);
}
.sol-glow-tl {
  position: absolute;
  top: -120px;
  right: -60px;
  width: 380px;
  height: 380px;
  border-radius: 50%;
  background: rgba(24, 183, 214, .18);
  filter: blur(80px);
  pointer-events: none;
}
.sol-glow-br {
  position: absolute;
  bottom: -100px;
  left: -80px;
  width: 320px;
  height: 320px;
  border-radius: 50%;
  background: rgba(0, 18, 92, .06);
  filter: blur(70px);
  pointer-events: none;
}

.sol-inner { position: relative; max-width: 860px; }
.sol-eyebrow { color: var(--cyan); }
.sol-eyebrow::before { background: var(--cyan); }

.sol-hero h1 {
  max-width: 780px;
  margin-top: 18px;
  font-size: clamp(36px, 4.8vw, 62px);
  line-height: 1.08;
}
.sol-lead {
  max-width: 640px;
  margin-top: 18px;
  font-size: 18px;
  line-height: 1.65;
  color: var(--text-muted);
}

/* Category pills */
.sol-pills {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 32px;
}
.sol-pill {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 7px 14px;
  border-radius: 8px;
  font-size: 13px;
  font-weight: 600;
  color: var(--primary);
  background: #fff;
  border: 1px solid var(--border);
  box-shadow: var(--shadow-sm);
  transition: border-color var(--dur-fast), color var(--dur-fast), transform var(--dur-fast), box-shadow var(--dur-fast);
}
.sol-pill :global(svg) { color: var(--cyan); flex-shrink: 0; }
.sol-pill:hover {
  border-color: var(--cyan);
  color: var(--primary);
  transform: translateY(-1px);
  box-shadow: var(--shadow);
}

/* Metrics */
.sol-metrics {
  display: flex;
  flex-wrap: wrap;
  gap: 0;
  margin-top: 44px;
  padding-top: 28px;
  border-top: 1px solid var(--border-soft);
  max-width: 680px;
}
.sol-metric {
  display: flex;
  flex-direction: column;
  gap: 3px;
  padding: 0 36px 0 0;
  margin-right: 36px;
  border-right: 1px solid var(--border);
}
.sol-metric:last-child { border-right: none; padding-right: 0; margin-right: 0; }
.sol-metric-val {
  font-family: "JetBrains Mono", monospace;
  font-size: 32px;
  font-weight: 800;
  color: var(--primary);
  line-height: 1;
  letter-spacing: -.02em;
}
.sol-metric-label {
  font-size: 12.5px;
  color: var(--text-muted);
  font-weight: 500;
}
.sol-metric-sub {
  font-size: 11px;
  font-weight: 700;
  color: var(--cyan);
  letter-spacing: .06em;
  text-transform: uppercase;
}

/* ===== Cards section ===== */
.hub-list { padding: 40px 0 80px; }

.solutions-grid {
  display: grid;
  grid-template-columns: repeat(12, minmax(0, 1fr));
  gap: 20px;
  align-items: start;
}
.solution-tile {
  min-width: 0;
}
.solution-tile:not(.solution-main) :global(.hub-card) {
  height: 100%;
}
.solution-main {
  grid-column: 1 / 9;
  grid-row: span 2;
  align-self: start;
}
.solution-side {
  grid-column: 9 / 13;
}
.solution-banner {
  grid-column: 1 / -1;
}
/* Rox banner — yatay düzen: metin solda, CTA sağda ortalı */
.solution-banner :global(.hub-card) {
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: center;
  column-gap: 40px;
  padding: 30px 34px;
}
.solution-banner :global(.card-head) { grid-area: 1 / 1 / 2 / 2; margin-bottom: 0; }
.solution-banner :global(.hub-card h2) { grid-area: 2 / 1 / 3 / 2; }
.solution-banner :global(.hub-card p) { grid-area: 3 / 1 / 4 / 2; max-width: 640px; }
.solution-banner :global(.card-link) {
  grid-area: 1 / 2 / 4 / 3;
  align-self: center;
  justify-self: end;
  margin-top: 0;
  padding-top: 0;
}

.solution-side :global(.card-visual) {
  margin-top: 2px;
  border-radius: 8px;
  background: #f6f9fc;
  overflow: hidden;
}
.solution-side :global(.card-visual img),
.solution-side :global(.card-visual video) {
  width: 100%;
  aspect-ratio: 16 / 7;
  object-fit: cover;
  object-position: center;
}
.p2l-card-visual {
  filter: saturate(.95) contrast(1.02);
}
.asset-mini-visual {
  position: relative;
  min-height: 132px;
  padding: 16px;
  border: 1px solid rgba(0, 18, 92, .08);
  background:
    radial-gradient(220px 120px at 88% 16%, rgba(24, 183, 214, .18), transparent 64%),
    linear-gradient(135deg, #f8fbff 0%, #edf4f9 100%);
  overflow: hidden;
}
.asset-mini-visual::before {
  content: "";
  position: absolute;
  inset: auto -20px -46px auto;
  width: 160px;
  height: 120px;
  border-radius: 50%;
  border: 1px solid rgba(0, 18, 92, .08);
  transform: rotate(-18deg);
}
.asset-panel {
  position: relative;
  display: grid;
  grid-template-columns: 74px minmax(0, 1fr);
  gap: 12px;
  align-items: center;
  height: 100%;
}
.asset-qr {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 5px;
  aspect-ratio: 1;
  padding: 9px;
  border-radius: 8px;
  background: #fff;
  border: 1px solid rgba(0, 18, 92, .12);
  box-shadow: 0 12px 26px rgba(0, 18, 92, .08);
}
.asset-qr span {
  border-radius: 2px;
  background: rgba(0, 18, 92, .78);
}
.asset-qr span:nth-child(2),
.asset-qr span:nth-child(4),
.asset-qr span:nth-child(9) {
  background: rgba(24, 183, 214, .82);
}
.asset-list {
  display: grid;
  gap: 7px;
}
.asset-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  min-height: 28px;
  padding: 6px 8px;
  border-radius: 7px;
  color: rgba(0, 18, 92, .72);
  background: rgba(255, 255, 255, .72);
  border: 1px solid rgba(0, 18, 92, .07);
  font-size: 10px;
  font-weight: 800;
  line-height: 1;
}
.asset-row strong {
  color: rgba(0, 18, 92, .86);
  font-size: 9px;
}
.asset-row.active {
  color: var(--primary);
  background: #fff;
  box-shadow: 0 10px 22px rgba(0, 18, 92, .08);
}
.asset-row.active strong {
  color: var(--cyan);
}

@media (max-width: 1000px) {
  .solution-main,
  .solution-side,
  .solution-banner {
    grid-column: span 6;
    grid-row: auto;
  }
  .solution-main,
  .solution-banner {
    grid-column: 1 / -1;
  }
}
@media (max-width: 760px) {
  .sol-hero { padding: 72px 0 52px; }
  .sol-metrics { flex-direction: column; gap: 20px; }
  .sol-metric { border-right: none; border-bottom: 1px solid var(--border-soft); padding: 0 0 20px; margin: 0; }
  .sol-metric:last-child { border-bottom: none; padding-bottom: 0; }
  .solution-main,
  .solution-side,
  .solution-banner {
    grid-column: 1 / -1;
  }
  /* Banner mobilde dikey istif */
  .solution-banner :global(.hub-card) {
    grid-template-columns: 1fr;
  }
  .solution-banner :global(.card-head),
  .solution-banner :global(.hub-card h2),
  .solution-banner :global(.hub-card p),
  .solution-banner :global(.card-link) {
    grid-area: auto;
  }
  .solution-banner :global(.hub-card p) { max-width: 100%; }
  .solution-banner :global(.card-link) { justify-self: start; align-self: start; }
}
</style>
